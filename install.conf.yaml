- clean: ['~']

- link:
    ~/.bashrc: .bashrc
    ~/.emacs: .emacs
    ~/emacs.d: emacs.d
    ~/.gitconfig: .gitconfig
    ~/.emacs.d: .emacs.d
    
- shell:
  - [git submodule update --init --recursive, Installing submodules]

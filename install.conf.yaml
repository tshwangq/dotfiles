- clean: ['~']

- link:
    ~/.bashrc: .bashrc
    ~/.emacs: .emacs
    ~/emacs.d: emacs.d
    ~/.gitconfig: .gitconfig
    
- shell:
  - [git submodule update --init --recursive, Installing submodules]
